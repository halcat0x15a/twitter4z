package twitter4z.http

import scalaz._
import Scalaz._

//sealed abstract class OptionTokens(val value: Option[Tokens]) extends NewType[Option[Tokens]]

//case class SomeTokens(tokens: Tokens) extends OptionTokens(Some(tokens))

//case object NoneTokens extends OptionTokens(None)
